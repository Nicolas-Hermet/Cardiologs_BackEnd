<!DOCTYPE html>
<html>
  <head>
    <title>Add your Delineation</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
  
    <h1>Delineation App</h1>
    <p>Upload your CSV file to delineate</p>
    <form action='/delineations/new/' enctype="multipart/form-data" method='POST'>
        
      <label for="ecg">Upload your CSV file</label>
      <input type='file' name='ecg' required/><br/>
      
      <label for="recorded-at">Set the start date and time of the recording.:</label>
      <input type="datetime-local" name="recorded-at" >

      <input type='submit' value='Upload'/>
    </form>

    <% if @ecg %>
      <h2>Delineation results </h2> 
      <ul>
        <li>Number of premature P waves : <%= @ecg.premature_p_wave_number%></li>
        <li>Number of premature QRS waves : <%= @ecg.premature_qrs_wave_number%></li>
        <li>Max rate : <%= @ecg.max_rate.first %> bpm <br> <% @ecg.max_rate.last.each do |time| %> at: <%= time %>; <br> <%end%> </li>
        <li>Min rate : <%= @ecg.min_rate.first %> bpm <br> <% @ecg.min_rate.last.each do |time| %> at: <%= time %>; <br> <%end%> </li>
        <li>Mean rate : <%= @ecg.mean_rate %> bpm</li>
      </ul>
    <% end %>

  </body>
</html>
